apiVersion: batch/v1
kind: CronJob
metadata:
  name: careeros-scraper
  namespace: careeros
spec:
  schedule: "0 * * * *"       # every hour on the hour
  concurrencyPolicy: Forbid   # never run two scrapers at once
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: scraper
              image: ghcr.io/iMeet07/careeros-scraper:latest
              command: ["python", "-m", "scraper.main", "--sources", "greenhouse", "lever"]
              envFrom:
                - configMapRef:
                    name: careeros-config
                - secretRef:
                    name: careeros-secrets
              volumeMounts:
                - name: config
                  mountPath: /app/config.json
                  subPath: config.json
              resources:
                requests:
                  cpu: "100m"
                  memory: "128Mi"
                limits:
                  cpu: "500m"
                  memory: "256Mi"
          volumes:
            - name: config
              configMap:
                name: careeros-scraper-config
---
# Store the user's config.json as a ConfigMap
# To update: kubectl create configmap careeros-scraper-config --from-file=config.json -n careeros
apiVersion: v1
kind: ConfigMap
metadata:
  name: careeros-scraper-config
  namespace: careeros
data:
  config.json: |
    {
      "profile": {
        "keywords": ["software engineer", "full stack", "backend"],
        "preferred_locations": ["Remote"],
        "remote_preferred": true,
        "greenhouse_boards": ["airbnb", "figma", "notion", "stripe"],
        "lever_companies": ["netflix", "spotify", "shopify", "github"]
      }
    }
